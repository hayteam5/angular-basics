import { Style2Directive } from './style2.directive';

describe('Style2Directive', () => {
  it('should create an instance', () => {
    const directive = new Style2Directive();
    expect(directive).toBeTruthy();
  });
});
